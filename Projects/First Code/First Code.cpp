// Include necessary libraries
#include <iostream>

// Use standard namespace for convenience
using namespace std;

int main()
{
    // Declare variables
    int a, b;  // Variables to store input values

    // Read two integer inputs from the user
    cin >> a;
    cin >> b;

    // Output the values in reverse order
    cout << b << "\t" << a << "\n";

    // Print "Lily"
    cout << "Lily";

    return 0;
}
